This folder contains a log that satisfies at least one "immediately
followed by" property type, as well as the arguments to texada and
expected instantiations returned from the texada run.

This property says that when some event x occurs, another event y must
always occur as the next event.
